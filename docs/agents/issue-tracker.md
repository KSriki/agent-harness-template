# Issue tracker — this repo

**Tracker:** Beads (`bd`) — agent-heavy solo repo, per the adopted preset
(commit dc62b16). Initialized 2026-08-18.

**Labels:** `needs-triage` · `needs-info` · `ready-for-agent` · `ready-for-human`
· `wontfix`.

**Boundary:** Beads is the WORK GRAPH only — learnings stay in
`learnings.jsonl`, run state stays in `.orchestrator/` (never `bd remember`).

> NOTE: this file is slated to become the *Tracker* section of
> `docs/agents/harness-config.md` (see `harness-prd-setup-harness.md`).
